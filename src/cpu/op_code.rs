#[derive(Debug, Clone, Eq, PartialEq)]
pub enum OpCode {
    ADC,
    BNE,
    BPL,
    BRK,
    CLC,
    CLD,
    CLI,
    CPX,
    DEX,
    DEY,
    INX,
    JMP,
    LDA,
    LDX,
    LDY,
    NOP,
    SBC,
    SEC,
    SED,
    SEI,
    STA,
    STX,
    TXS,
}
