// RustNES UI Components
//
// This crate provides user interface components for the RustNES emulator.
// It depends on the rn_core crate for emulation functionality.

pub mod widgets;


#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
